import lejos.nxt.Motor;
//import lejos.nxt.*

class Driver
{
	/** Constructor, sets the speed to run at */
	Driver( int speed )
	{
	}

	/** getReady venter til der gives signal til at starte */
	void getReady()
	{
	}

	/** Turns right if true */
	void turn(boolean right)
	{
	}

	/** instructs the tron bike to go forward
	 * should do this almost all the time */
	void goForward()
	{
	}

	/** routine for terminating the tron bike */
	void die()
	{
	}
}
